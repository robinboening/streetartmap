.map[map]

.location
  a.entry[ng-repeat="location in locations" href="/locations/{{location.id}}" ng-mouseover="markerPopup(location.id)"]
    img ng-src="{{location.thumbnail}}"
    p
      | {{location.fullAddress}}<br>Uploaded at: {{location.createdAt}}
